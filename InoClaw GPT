下面这版是重新整理后的**完整总结版**，把你后面补充的信息都合进去了：你是**前端主导**，团队 5 人，项目是**自研搭建**，不是二开；后端 gateway 是后端写的，你负责前端对接；项目在集团内部几百人使用；你前端基本都有参与；性能优化重点是**侧边栏历史列表 + 对话页历史消息虚拟列表**。

**一、项目最终定位**

项目可以定义为：

```text
自研 AI Agent 桌面工作台前端项目
```

更完整的项目描述：

```text
基于 React 19 + TypeScript + Vite + Tauri 2 自研搭建的 AI Agent 桌面客户端，面向集团内部数百用户使用，支持 AI 实时对话、Agent 管理、Skills/MCP 能力管理、Cron 定时任务、会话历史、使用统计、系统设置、飞书登录和桌面托盘提醒等能力。
```

面试一句话：

```text
这是一个自研的 AI Agent 桌面工作台，我在 5 人团队中担任前端主导，主要负责前端整体功能落地和核心模块开发，包括对话页、Agent 管理、定时任务、WebSocket gateway 对接和长列表性能优化。
```

注意口径：

```text
gateway 后端不是你写的，是后端同学写的。
你负责的是前端对接、通信封装、事件处理和页面状态同步。
```

**二、你的真实背景信息**

```text
角色：前端主导
团队规模：5 人
项目周期：2026 年春节后 - 至今，约半年，仍在持续迭代
项目状态：集团内部使用，用户量几百人
项目性质：自研搭建，不是二次开发
主要负责：前端整体参与，重点是对话页面、Agent 管理、Cron 定时任务、gateway 前端对接、虚拟列表性能优化
开发方式：大量使用 AI 编程工具辅助开发，你负责需求拆解、代码审查、调试验证和功能集成
```

最稳的自我定位：

```text
我是这个项目的前端主导，前端大部分模块都有参与，重点负责对话页、Agent 管理、定时任务和 gateway 前端对接。后端 gateway 由后端同学实现，我这边负责前端通信层封装和业务页面接入。
```

**三、技术栈**

```text
前端框架：React 19、TypeScript、Vite、React Router
UI 与样式：Ant Design、Less、CSS 变量、浅色/深色主题
桌面端：Tauri 2、Rust
通信：WebSocket RPC、Tauri invoke、本地 gateway
状态管理：React local state、Context、自研 createAppStore
国际化：i18next、react-i18next、中/英/日资源
图表：ECharts
富文本：react-markdown、Mermaid、KaTeX、highlight.js
监控：Sentry
桌面能力：系统托盘、通知、窗口控制、文件读取、应用更新、单实例、开机启动
性能优化：动态高度虚拟列表、ResizeObserver、requestAnimationFrame、二分查找可见区间、滚动补偿
```

**四、项目核心链路**

你一定要背熟这条：

```text
用户在 React 页面操作
-> consoleApi 统一业务调用
-> gatewayRpc 维护 WebSocket RPC 连接
-> 后端 gateway / 本地 inoclaw-service 执行 AI、Agent、MCP、Cron 等能力
-> gateway 通过 WebSocket 推送实时事件
-> 前端根据 sessionKey、runId、messageId 更新 chatStore 和页面 UI
-> Tauri 提供桌面端窗口、托盘、文件、更新等能力
```

解释版：

```text
页面不直接写 gateway method 字符串，而是通过 consoleApi 调业务方法；consoleApi 再调用 gatewayRpc；gatewayRpc 负责 WebSocket 连接、握手、请求响应匹配、事件订阅、JWT 失效和重连。这样页面层只关心业务语义，底层通信逻辑集中维护。
```

**五、项目核心价值**

这个项目不是普通后台，也不是单一聊天页，它的核心价值是：

```text
把 AI Agent 的对话、配置、工具、定时运行和桌面提醒做成一个统一的桌面工作台。
```

可以提炼成五个关键词：

```text
实时对话
Agent 管理
定时运行
桌面能力
可扩展工具体系
```

展开讲：

```text
实时对话：支持 AI 流式响应、停止回答、重试、编辑、历史回填、多会话切换。
Agent 管理：支持 Agent 创建、配置、模型/工具/技能关联。
定时运行：Cron 自动触发 Agent run，并沉淀会话历史。
桌面能力：Tauri 提供托盘提醒、窗口、文件、本地服务和更新能力。
可扩展工具体系：Skills、MCP、插件市场扩展 Agent 能力。
```

**六、简历标准版**

```text
InoClaw AI Agent 桌面工作台 | 前端主导
项目周期：2026.02 - 至今 | 团队规模：5人 | 项目状态：集团内部数百用户使用，持续迭代
技术栈：React 19、TypeScript、Vite、Tauri 2、Ant Design、WebSocket、i18next、ECharts、Markdown/Mermaid/KaTeX

项目描述：
自研 AI Agent 桌面客户端，面向集团内部数百用户使用，支持 AI 实时对话、Agent 管理、定时任务、Skills/MCP 插件管理、会话历史、使用统计、系统设置和桌面托盘提醒等能力。前端通过 WebSocket RPC 对接后端 gateway，并结合 Tauri 提供桌面端能力。

个人职责：
- 担任项目前端主导，参与前端从 0 到 1 搭建和持续迭代，负责对话页、Agent 管理、Cron 定时任务、gateway 前端对接和性能优化等核心模块。
- 负责 AI 对话页核心交互与状态管理，支持流式响应、停止回答、失败重试、会话切换、历史回填、附件处理和快捷指令，通过 sessionKey、runId、messageId 保证多会话实时事件归属正确。
- 针对长对话和历史列表进行性能优化，自研动态高度虚拟列表，结合高度预估、ResizeObserver 实测、signature 缓存、requestAnimationFrame 批处理、二分查找可见区间和滚动补偿，改善长列表滚动和流式输出体验。
- 参与前端 WebSocket Gateway RPC 对接，封装 gatewayRpc 和 consoleApi 业务门面，统一处理请求响应匹配、实时事件订阅、连接状态、JWT 失效和断线重连，降低页面层对底层协议的耦合。
- 负责 Agent 管理和 Cron 定时任务模块开发，支持 Agent 配置、模型/工具/技能关联、任务创建编辑、手动执行、执行历史查看，并处理 Cron run 与聊天会话、托盘提醒之间的联动。
- 参与 Tauri 桌面能力前端接入，支持本地 gateway 服务状态、窗口交互、系统托盘会话完成提醒、文件能力和应用更新等桌面端场景。
```

**七、简历精简版**

如果简历空间有限，用这 5 条：

```text
- 前端主导参与自研 AI Agent 桌面工作台开发，项目基于 React 19 + TypeScript + Tauri，服务集团内部数百用户，负责对话页、Agent 管理、定时任务、gateway 前端对接和性能优化等核心模块。
- 负责 AI 对话页核心交互与状态管理，支持流式响应、停止回答、失败重试、会话切换、历史回填和附件处理，通过 sessionKey、runId、messageId 保证多会话实时事件归属正确。
- 针对长对话和历史列表进行性能优化，自研动态高度虚拟列表，结合高度预估、ResizeObserver 实测、signature 缓存、rAF 批处理、二分查找和滚动补偿，改善长列表滚动和流式输出体验。
- 参与前端 WebSocket Gateway RPC 对接，封装 gatewayRpc 和 consoleApi 业务门面，统一处理请求响应匹配、实时事件订阅、连接状态、JWT 失效和断线重连。
- 负责 Agent 管理和 Cron 定时任务模块开发，支持 Agent 配置、模型/工具/技能关联、任务创建编辑、手动执行、执行历史查看，并处理 Cron run 与聊天会话、托盘提醒之间的联动。
```

**八、岗位侧重点**

前端岗：

```text
复杂对话页面、React Hooks、状态分层、动态高度虚拟列表、WebSocket 实时事件、Ant Design 页面开发、多语言和主题适配。
```

桌面端岗：

```text
Tauri 桌面客户端、前端对接本地 gateway、系统托盘、窗口交互、文件读取、应用更新、本地服务状态展示。
```

AI 应用开发岗：

```text
AI Agent 工作台、流式对话、Agent 管理、Skills/MCP 管理、Cron 自动触发 Agent run、会话历史和运行状态管理。
```

**九、项目难点和亮点**

1. AI 对话页复杂状态一致性

难点：

```text
AI 对话不是普通 HTTP 请求，而是 WebSocket 实时事件持续回流。用户可能发送消息、停止回答、切换会话、重试、编辑消息、历史回填，同时 Cron 定时任务和 Agent run 也可能产生新事件。
```

容易出的问题：

```text
A 会话回复显示到 B 会话
旧 run 事件覆盖新消息
停止按钮假忙
final 后尾流继续更新
历史回填覆盖实时消息
```

解决思路：

```text
通过 sessionKey、runId、messageId、clientAssistantMessageId 判断事件归属。当前可见会话只消费属于自己的实时事件；收到 final 后清理 runId、loading 和 dispatch guard；晚到事件或非当前会话事件不能直接污染当前消息列表。
```

2. 动态高度虚拟列表性能优化

难点：

```text
聊天消息高度不固定，可能包含文本、Markdown、代码块、图片、工具调用结果；AI 流式输出时高度还会持续变化。
```

实现亮点：

```text
- 36 条以上消息启用虚拟化
- 根据文本、代码块、图片、Markdown 内容预估高度
- ResizeObserver 实测高度
- signature 缓存高度，避免重复测量
- requestAnimationFrame 批量 flush 测量结果
- 二分查找定位可见区间
- overscan 预渲染
- 滚动位置补偿，避免高度修正后页面跳动
- 智能吸底：用户在底部时自动滚动，用户上滑后不强制拉回
```

面试一句话：

```text
这个虚拟列表难点不只是只渲染可见项，而是聊天消息高度会随着流式输出动态变化，还要保证不跳动、不乱吸底。
```

3. WebSocket Gateway RPC 前端对接

难点：

```text
前端不是普通 REST 请求，而是通过 WebSocket RPC 对接后端 gateway，既有请求响应，也有实时事件推送。
```

前端侧处理：

```text
connect.challenge / connect 握手
请求 id 生成
pending response 匹配
事件订阅和分发
JWT 失效处理
自动重连
连接状态同步
请求失败清理
```

口径：

```text
gateway 后端由后端同学实现，前端负责 gatewayRpc 封装、consoleApi 门面、页面接入和事件状态同步。
```

4. Tauri 桌面能力接入

难点：

```text
项目不是普通 Web，需要接入窗口、托盘、文件、本地服务、更新等桌面能力。
```

前端侧参与：

```text
Tauri invoke 调用桌面能力
本地 gateway 启动/探活状态展示
系统托盘提醒
点击托盘提醒恢复主窗口并跳转会话
文件读取/附件处理
应用更新状态展示
```

5. 托盘会话完成提醒

难点：

```text
只有窗口隐藏到托盘后才提醒；普通聊天、Agent run、Cron run 都可能触发完成事件；提醒点击后要恢复窗口并跳转对应会话；提醒消费后不能重复出现。
```

亮点：

```text
提醒列表需要单一权威数据源，避免前端、Tauri、popup 各存一份导致旧消息重复、闪烁不停止或状态不同步。
```

6. Agent 管理和 Cron 联动

Agent 管理不是简单 CRUD：

```text
它会关联模型、工具、Skills、MCP、Cron、市场插件和会话。
```

Cron 也不是普通任务列表：

```text
Cron 会自动触发 Agent run，并产生会话历史和运行事件，所以要和聊天历史、Agent 状态、托盘提醒联动。
```

**十、状态管理为什么这样设计**

项目用了：

```text
React local state
Context
自研 createAppStore
```

原因：

```text
状态类型不同，不能全部塞进一个全局 store。
```

分层：

```text
React local state：弹窗、表单、输入框、筛选、loading 等局部 UI 状态。
Context：认证、主题、语言、工作台布局等应用级上下文。
createAppStore：聊天消息、当前会话、runId、排队消息、Agent 页面共享状态等跨组件且高频变化的业务状态。
```

面试回答：

```text
我们按状态作用范围分层。局部 UI 状态用 local state，应用级上下文用 Context，高频跨组件业务状态用 createAppStore。这样能保持状态边界清晰，避免 Context 因聊天流式更新导致大范围重渲染，也不用为了少量共享状态引入 Redux 这类较重方案。
```

**十一、面试高频问题和答案**

Q1：为什么用 Tauri，而不是普通 Web 或 Electron？

```text
普通 Web 缺少桌面能力，比如本地服务状态、系统托盘、文件读取、开机启动、应用更新和单实例运行。Electron 可以做，但包体和资源占用更大。Tauri 复用系统 WebView，Rust 侧负责桌面能力，更轻量，也适合 React UI + 本地服务桥接的场景。
```

Q2：对话页面的流式响应怎么实现？

```text
用户发送消息后，前端先生成用户消息和 assistant 占位消息，再通过 gatewayRpc 发送 chat.send。后端 gateway 通过 WebSocket 持续推送 runtime event，前端订阅事件后，根据 sessionKey、runId 和 messageId 找到对应会话和消息，增量更新 assistant 内容。收到 final 事件后结束本轮 loading。
```

Q3：停止回答怎么做？怎么避免 UI 假停止？

```text
停止回答不是单纯关闭前端 loading，而是发送 chat.abort 通知后端停止当前 run。只有 abort 请求成功发出后，前端才清理当前会话的 runId、loading 和本地 guard。如果消息还没真正发出去，只是在本地 dispatch 阶段，则取消本地发送流程，避免后续继续发送。
```

Q4：WebSocket 断了怎么办？怎么重连？

```text
gatewayRpc 统一维护连接状态。断开后会标记 reconnecting，清理或 reject pending 请求，重新获取 gateway bootstrap，重新建立 WebSocket，并重新走 connect.challenge/connect 握手。如果是 JWT 失效，则清理登录态并跳转登录，而不是无限重连。同时设置最大连续重连次数，避免服务异常时无限重试。
```

Q5：会话切换时，怎么避免 A 会话的消息流到 B 会话？

```text
所有消息和事件都必须绑定 sessionKey、runId、clientAssistantMessageId 或 messageId。用户切换会话后，A 会话的事件如果继续回来，不能直接更新当前可见消息列表，而要根据事件归属更新对应会话缓存，或者等历史接口回填。
```

Q6：虚拟列表怎么做的？为什么不用现成库？

```text
项目中的聊天消息是动态高度，内容包括 Markdown、代码块、图片、工具调用结果，并且 AI 流式输出时高度会不断变化。普通固定高度虚拟列表不适合。我们做了高度预估、ResizeObserver 实测、signature 缓存、rAF 批处理、二分查找可见区间和滚动补偿，自研可以更好处理流式输出、智能吸底和滚动不跳动。
```

Q7：Agent 管理页面主要管理什么？

```text
Agent 管理页面主要管理 Agent 列表、创建、重命名、删除、详情配置、模型配置、工具/Skills 关联、市场安装卸载、Agent 文件配置和状态同步。它不是简单 CRUD，会和模型、工具、Skills、Cron、市场插件等模块联动。
```

Q8：定时任务和普通聊天有什么关系？

```text
普通聊天是用户手动输入触发 Agent run；定时任务是 Cron 到时间后自动触发 Agent run。两者底层都可能产生会话记录和运行事件，所以定时任务执行完后，聊天历史、托盘提醒和 Cron 执行记录都可能需要刷新。
```

Q9：AI 生成的代码你怎么保证质量？

```text
我会先理解需求和现有模块边界，再让 AI 辅助生成初版实现。生成后人工检查代码是否符合项目分层、状态是否正确、异步边界是否覆盖，比如 loading、runId、pending、abort、会话切换等。最后通过运行、构建和手动场景验证确认功能可用。
```

Q10：这个项目最难的问题是什么？

```text
最难的是对话页状态一致性。AI 对话是 WebSocket 事件持续回流，用户可能切换会话、停止回答、重试、编辑消息，同时 Cron 也可能触发新的 Agent run。如果只按当前页面状态处理事件，就容易出现 A 会话回复显示到 B 会话、停止按钮假忙、旧 run 事件覆盖新消息等问题。解决思路是用 sessionKey、runId、messageId 判断事件归属，并在 final 事件后及时清理 loading 和本地 guard。
```

**十二、延伸问题和答案**

Q：为什么要有 consoleApi？

```text
consoleApi 是业务 API 门面。页面不直接写 gateway method 字符串，而是调用 consoleApi.agents.list、consoleApi.sessions.list、consoleApi.chat.history 这种业务方法。这样页面更清晰，协议变化时只需要改 API 层。
```

Q：gatewayRpc 主要做什么？

```text
gatewayRpc 是 WebSocket RPC 底层客户端，负责连接 gateway、握手、请求 id 生成、pending 请求匹配、事件订阅、JWT 失效处理、断线重连、连接状态通知和请求失败清理。
```

Q：为什么不用 Redux？

```text
项目目前没有复杂到必须引入 Redux。聊天、Agent 等共享状态用自研 createAppStore 已经能满足 getState、setState、selector 订阅，代码更轻，依赖更少。
```

Q：为什么不用 Context 管聊天消息？

```text
聊天消息是高频更新状态，流式响应时会不断变化。如果全部放 Context，Provider value 一变可能导致大范围重渲染。createAppStore 可以用 selector 精准订阅，性能和边界更好。
```

Q：为什么需要 runId？

```text
runId 用来标识一次 Agent 运行。一个会话里可能有多次运行，也可能有晚到事件。用 runId 可以判断事件属于哪一次回复，避免旧 run 污染新消息。
```

Q：为什么需要 clientAssistantMessageId？

```text
前端发送消息时会先创建 assistant 占位消息，clientAssistantMessageId 用来把后端流式事件和这个占位消息对应起来，避免流式内容追加到错误消息上。
```

Q：final 事件为什么重要？

```text
final 表示当前 run 结束。收到 final 后，前端要清理 runId、loading、dispatch guard 等运行状态，避免停止按钮、光标或页面一直处于进行中。
```

Q：历史回填和实时流有什么冲突？

```text
实时流负责当前运行中的消息更新，历史回填负责从 chat.history 拉完整历史。如果处理不好，历史回填可能覆盖实时消息，或者旧数据覆盖新标题，所以要区分实时状态和历史数据来源。
```

Q：托盘提醒怎么实现？

```text
前端订阅 gateway 的完成事件，识别会话或 Cron run 完成后上报给 Tauri；Tauri 侧维护提醒队列、托盘闪烁和 popup 展示。用户点击提醒后，Tauri 恢复主窗口并通知前端跳转到对应会话。
```

Q：Agent、Skills、MCP 是什么关系？

```text
Agent 是具体 AI 助手或执行主体；Skills 是 Agent 可使用的技能扩展；MCP 是外部工具或服务接入协议。前端提供可视化管理，让用户安装、启用、配置这些能力，并关联到 Agent。
```

Q：性能优化没有量化指标怎么说？

```text
可以诚实说，当时主要以内部使用体验验收为主，通过长对话和历史列表滚动是否卡顿来判断。简历里可以写“改善长列表滚动和流式输出体验”，不要编具体百分比。如果后续继续优化，会补充 FPS、Long Task、首屏渲染耗时等指标监控。
```

Q：你是前端主导，但 gateway 是后端写的，怎么避免夸大？

```text
我会明确说 gateway 后端由后端同学实现，我负责前端对接和封装，包括 gatewayRpc、consoleApi、事件订阅、连接状态和页面状态同步。
```

**十三、AI 辅助开发怎么讲**

不要说：

```text
这个项目都是 AI 写的。
```

建议说：

```text
这个项目开发过程中大量使用 AI 编程工具辅助实现。我主要负责需求拆解、模块理解、提示词设计、代码审查、调试验证和前端功能集成。AI 提升了编码效率，但复杂链路，比如对话流式状态、会话切换、WebSocket 事件回流、虚拟列表滚动补偿和桌面端边界，仍然需要我理解业务后判断方案、定位问题和做验证。
```

如果面试官问“那你到底写了什么？”

```text
我的工作不是单纯手写每一行代码，而是负责把需求落到前端模块里：理解现有架构、拆分任务、让 AI 辅助生成初版实现，然后检查状态流、接口调用、异常场景和页面表现，最后运行项目进行调试和验证。
```

**十四、能力标签**

这个项目能体现你的能力：

```text
前端主导能力
复杂前端页面开发能力
AI 对话状态管理能力
WebSocket 实时通信对接能力
动态高度虚拟列表性能优化能力
桌面端前端接入能力
跨模块业务理解能力
Agent / Cron / MCP 等 AI 应用场景理解
AI 辅助开发下的代码审查和验证能力
```

**十五、真实性边界和红线**

可以说：

```text
自研搭建 AI Agent 桌面工作台
前端主导
前端基本都有参与
项目服务集团内部数百用户
负责对话页、Agent 管理、定时任务、gateway 前端对接、性能优化
大量使用 AI 工具辅助开发
```

不要说：

```text
后端 gateway 是我写的
整个项目我一个人独立完成
性能提升了具体百分比但没有数据
负责 Tauri/Rust 全部实现，如果你主要做前端接入
```

最稳底线：

```text
这是一个自研 AI Agent 桌面工作台项目，我作为前端主导参与前端从 0 到 1 搭建，主要负责对话页、Agent 管理、定时任务、gateway 前端对接和虚拟列表性能优化。后端 gateway 由后端同学实现，我负责前端通信封装和业务接入。
```

**十六、30 秒口述版**

```text
这个项目是我们自研搭建的 AI Agent 桌面工作台，基于 React 19、TypeScript 和 Tauri，服务集团内部数百用户。我在 5 人团队中担任前端主导，前端大部分模块都有参与，重点负责对话页、Agent 管理、定时任务、gateway 前端对接和性能优化。项目通过 WebSocket RPC 对接后端 gateway，支持 AI 流式对话、Agent 配置、Cron 定时运行、Skills/MCP 管理和桌面托盘提醒。最大的难点是对话页状态一致性和动态高度虚拟列表，因为 AI 回复是实时事件流，用户还可能切换会话、停止回答、重试，所以前端必须通过 sessionKey、runId、messageId 判断事件归属。
```

**十七、1 分钟口述版**

```text
这是一个自研 AI Agent 桌面工作台项目，前端基于 React 19、TypeScript、Vite，桌面端基于 Tauri。它不是普通 Web 后台，而是一个桌面客户端，支持 AI 对话、Agent 管理、定时任务、Skills/MCP 插件管理、会话历史、使用统计和系统托盘提醒，目前服务集团内部数百用户。

我在 5 人团队中担任前端主导，前端基本都有参与，重点负责对话页面、Agent 管理、Cron 定时任务、gateway 前端对接和性能优化。后端 gateway 是后端同学实现的，我负责前端 gatewayRpc、consoleApi 封装以及页面事件接入。

对话页这块比较复杂，用户发送消息后，前端会先创建用户消息和 assistant 占位消息，再通过 WebSocket RPC 发送 chat.send，后端事件流回来后，前端根据 sessionKey、runId 和 messageId 增量更新消息。这里最难的是状态一致性，比如会话切换、停止回答、重试、历史回填、Cron 触发 run，都可能导致事件归属混乱。

另外我也做了长对话性能优化，自研动态高度虚拟列表，通过高度预估、ResizeObserver 实测、signature 缓存、rAF 批处理和滚动补偿，改善长对话和历史列表的滚动体验。
```

**十八、最终定稿结论**

你这个项目最终应该这样讲：

```text
项目定义：自研 AI Agent 桌面工作台
你的角色：前端主导
团队规模：5 人
用户规模：集团内部数百用户
技术核心：React + TypeScript + Tauri + WebSocket RPC
业务核心：AI 对话、Agent 管理、Cron 定时任务、Skills/MCP、会话历史、托盘提醒
你的重点：对话页、Agent 管理、定时任务、gateway 前端对接、虚拟列表性能优化
最大难点：AI 对话实时事件下的多会话状态一致性
最大亮点：动态高度虚拟列表 + WebSocket 事件归属处理 + Tauri 桌面能力接入
```

最值得你反复准备的主线：

```text
AI 对话页状态一致性 + 动态高度虚拟列表性能优化 + WebSocket gateway 前端对接 + Tauri 桌面能力接入
```

这条主线最能体现你不是只会写 CRUD，而是能处理实时通信、复杂状态、性能优化和桌面端工程问题。
